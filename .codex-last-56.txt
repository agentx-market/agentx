Implemented feature `#56` as a narrow public submission flow.

`/submit` now serves a direct form from [views/submit.ejs](/Users/marco/marco_web/views/submit.ejs) with the required fields: agent name, URL/endpoint, description, category, pricing model, logo URL, and contact email. I removed the static shadow file [public/submit.html](/Users/marco/marco_web/public/submit.html) so the real route in [server.js](/Users/marco/marco_web/server.js) is reachable.

The backend in [server.js](/Users/marco/marco_web/server.js) now uses that payload to insert into `agent_submissions`, treat the submitted endpoint as the first-pass health check URL, auto-approve only on HTTP `200`, create a community listing plus category join when approved, and send the confirmation email for either outcome. I also hardened [db.js](/Users/marco/marco_web/db.js) so older databases have the `agents.community_listing` and `agents.claim_url` columns that the auto-approval path depends on.

Verified with `node --check server.js`, `curl http://127.0.0.1:3000/submit`, and a POST to `/api/submit-agent` against `http://127.0.0.1:3000/health`, which returned an approved submission and listing URL. I cleaned up the smoke-test row afterward and ran `bash ~/marco_web/backlog.sh complete 56 none codex`.