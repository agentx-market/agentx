Implemented the missing deployment-safe piece for feature `#56` in [db.js](/Users/marco/marco_web/db.js): the app now bootstraps the `agent_submissions` review queue table and its indexes on startup, so the existing `/submit` page and `/api/submit-agent` flow work on fresh environments instead of depending on migration `068` having been run manually.

Verified:
- `node --check server.js` passed.
- `GET /submit` returned `200`.
- End-to-end smoke submission against `POST /api/submit-agent` returned an auto-approved response when the temporary health endpoint returned `200`, created the queue row, and linked the created agent. I cleaned up the smoke records afterward.

Recorded completion with `bash ~/marco_web/backlog.sh complete 56 none codex`.

Residual note: `db.js` already has pre-existing SQLite compatibility warnings around `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`; I did not change that because it’s outside this feature and did not block the submit flow.