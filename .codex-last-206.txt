Implemented the registration repair in [server.js](/Users/marco/marco_web/server.js). `POST /api/agents` now requires an authenticated operator, creates an operator-owned pending agent, persists `slug` and `health_endpoint_url`, assigns the selected category, and returns the extra registration fields the flow expects. `/api/agents/:id/health-check` now performs a real probe before activation, records `agents_health_history`, and only promotes the agent to `active` when the endpoint is healthy. I also fixed the broken operator links in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs), [views/register.ejs](/Users/marco/marco_web/views/register.ejs), [views/dashboard.ejs](/Users/marco/marco_web/views/dashboard.ejs), and [views/status.ejs](/Users/marco/marco_web/views/status.ejs) so “Dashboard” lands on `/my-agents` instead of the static `/dashboard`.

The end-to-end smoke coverage is now in [scripts/smoke-agent-submission.js](/Users/marco/marco_web/scripts/smoke-agent-submission.js). It verifies GitHub OAuth redirect wiring, registers an agent through the API with a signed session cookie, runs the health check, confirms the listing appears in marketplace browse, and confirms it appears on `/my-agents`. I verified it by running `node scripts/smoke-agent-submission.js`, plus targeted `curl` checks against `/browse`, `/api/browse`, and `/my-agents`. The server was restarted and feature `206` was marked complete via `backlog.sh`.