Feature `#82` is now working end-to-end.

The repo already had the core browse waitlist banner implementation in place, so the only code change I made was to prevent the global newsletter sticky bar from rendering on `/browse` when the dedicated waitlist banner exists. That fix is in [public/js/main.js](/Users/marco/marco_web/public/js/main.js#L523). Without it, the newsletter CTA was intercepting clicks on the new waitlist bar.

Verification:
- Real browser check on `http://127.0.0.1:3000/browse`
- Hidden on initial load, visible after scrolling past 50%
- Submit succeeds and hides the bar afterward
- Email persisted to `waitlist` in `agentx.db`
- `node --check public/js/main.js` passed

Backlog status was updated with `bash ~/marco_web/backlog.sh complete 82 none codex`.