Feature `#82` is already implemented in the current branch state and matches the acceptance criteria.

`/browse` renders a non-modal sticky bottom email bar for logged-out users in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs), the client-side 50% scroll reveal, dismiss, and submit flow is in [public/js/browse.js](/Users/marco/marco_web/public/js/browse.js), and the banner styling is in [public/css/browse.css](/Users/marco/marco_web/public/css/browse.css). Signups post to the existing `POST /api/waitlist` handler in [server.js](/Users/marco/marco_web/server.js), which writes to the `waitlist` table defined in [migrations/004_waitlist.sql](/Users/marco/marco_web/migrations/004_waitlist.sql).

Verification passed with `node scripts/test-feature82-waitlist-banner.js`, which confirmed:
- `/browse` includes the banner markup and `data-scroll-threshold="0.5"`
- `POST /api/waitlist` stores the submitted email
- duplicate submissions return the expected idempotent response

I did not edit any of the protected paths. I also ran `bash ~/marco_web/backlog.sh complete 82 none codex`, and the item is marked complete.