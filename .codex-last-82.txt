Implemented the narrow backend piece for feature `#82` in [server.js](/Users/marco/marco_web/server.js#L1393). The `/browse` banner flow already existed in the working tree; I kept that intact and patched `POST /api/waitlist` so duplicate banner submissions return success without re-sending welcome email, Telegram notifications, or signup outcome logs.

Verified locally:
- `node --check server.js` passed.
- `curl http://127.0.0.1:3000/browse` includes the logged-out banner markup with `data-scroll-threshold="0.5"`.
- Playwright confirmed the banner is hidden on load, becomes visible after scrolling past 50%, and dismiss sets `browseWaitlistBannerDismissed=1`.
- `POST /api/waitlist` returns `Added to waitlist` on first submit and `Already on waitlist` on repeat submit for the same email.

I also updated [memory/AGENT-MEMORY.md](/Users/marco/marco_web/memory/AGENT-MEMORY.md) and marked backlog item `#82` complete.