Implemented the `/browse` guest lead-capture pass without touching the protected dirty paths. The banner remains logged-out-only, sticky at the bottom, dismissible, and tied to the existing `waitlist` insert path; I tightened it by moving the scroll threshold and source path into the banner markup so the client no longer hardcodes those values in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs) and [public/js/browse.js](/Users/marco/marco_web/public/js/browse.js).

Verified locally by:
- loading `http://127.0.0.1:3000/browse` and confirming the guest banner shell renders with `data-scroll-threshold="0.5"` and `data-source-path="/browse"`
- exercising the real browser scroll reveal path
- posting to `POST /api/waitlist` with `sourcePath=/browse` and receiving `{"status":"ok","message":"Added to waitlist"}`

I restarted the local server and ran `bash ~/marco_web/backlog.sh complete 82 none codex`.