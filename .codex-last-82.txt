Feature `#82` is in place on `/browse` for logged-out visitors: the page renders a dismissible bottom email bar, the client reveals it at 50% scroll and submits to `/api/waitlist`, and the server persists emails in `waitlist`.

I kept the pass narrow because the implementation was already present in the dirty browse files. The only code change I made was adding a regression check in [scripts/test-browse.js](/Users/marco/marco_web/scripts/test-browse.js) so the smoke test now verifies both the browse API and the logged-out lead-capture banner markup.

Verification:
- `curl http://127.0.0.1:3000/browse` returned `200` and included `emailBanner`, `data-scroll-threshold="0.5"`, and `emailBannerForm`.
- `node scripts/test-browse.js` passed.
- `bash ~/marco_web/backlog.sh complete 82 none codex` completed successfully.