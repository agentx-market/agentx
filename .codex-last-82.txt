The `/browse` lead-capture flow is in place and now reads as a subtler sticky bottom bar. I softened the banner styling in [public/css/browse.css](/Users/marco/marco_web/public/css/browse.css#L590) so the logged-out email capture feels like a bottom banner instead of a heavier popup treatment.

I verified the existing wiring is present end to end: the logged-out-only banner markup is in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs#L221), the 50% scroll reveal, dismiss, and submit behavior is in [public/js/browse.js](/Users/marco/marco_web/public/js/browse.js#L494), and submissions insert into the `waitlist` table via [server.js](/Users/marco/marco_web/server.js#L1393). I also confirmed the rendered `/browse` HTML includes the banner text, `data-scroll-threshold="0.5"`, and `/api/waitlist` target with `curl`.

Feature `82` has been marked complete with `bash ~/marco_web/backlog.sh complete 82 none codex`.