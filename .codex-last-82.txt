Implemented the `/browse` lead-capture banner path and tightened the backend write behavior.

The sticky bottom banner for logged-out visitors is already rendered in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs#L220) and triggered at 50% scroll in [public/js/browse.js](/Users/marco/marco_web/public/js/browse.js#L489). I made two targeted fixes around it:
- [server.js](/Users/marco/marco_web/server.js#L1116): normalize waitlist emails with `trim().toLowerCase()` before validation, insert, welcome email, and logging.
- [public/css/browse.css](/Users/marco/marco_web/public/css/browse.css#L560): add `position: relative` to the banner container so the dismiss button positions correctly on mobile.

Verified with:
- `node --check server.js`
- `curl http://127.0.0.1:3000/browse` returning `200` and including the banner markup
- `POST /api/waitlist` returning `200`
- SQLite row written as normalized lowercase after restart

I also restarted the local server and marked backlog item `82` complete.