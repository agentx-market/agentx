Feature #82 is in place on `/browse` for logged-out visitors: the sticky dismissible bottom bar appears after 50% scroll, posts to `/api/waitlist`, and continues to store emails in the existing `waitlist` table. I kept the existing browse-banner implementation in the working tree and added one narrow backend improvement so browse submissions are attributed correctly in outcome logging.

The only code I changed was [server.js](/Users/marco/marco_web/server.js) and [browse.js](/Users/marco/marco_web/public/js/browse.js): the waitlist endpoint now accepts an optional `sourcePath`, and the browse banner submits `sourcePath: '/browse'`.

Verified with `node --check server.js`, `node --check public/js/browse.js`, and `npm run smoke:browse`. I also ran `bash ~/marco_web/backlog.sh complete 82 none codex`.