Feature `#82` is in place and closed.

The browse page already had the lead-capture implementation in the current worktree: logged-out users on `/browse` get a dismissible sticky bottom banner after 50% scroll, and submissions post to `/api/waitlist` with `sourcePath=/browse`. I made one narrow hardening edit in [views/browse.ejs](/Users/marco/marco_web/views/browse.ejs) to align the email field with server validation by adding `inputmode="email"`, `spellcheck="false"`, and `maxlength="254"`.

Verification passed locally. `curl http://127.0.0.1:3000/browse` returned the banner markup, and a live `POST /api/waitlist` with a reserved `.invalid` test address returned `{"status":"ok","message":"Added to waitlist"}`; a follow-up DB check confirmed the row exists in `waitlist`. I also updated [memory/AGENT-MEMORY.md](/Users/marco/marco_web/memory/AGENT-MEMORY.md) and cleared the checkpoint.